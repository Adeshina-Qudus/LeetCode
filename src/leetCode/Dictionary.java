package leetCode;

public class Dictionary {


    public static String dictionary(String[] dictionary, String givenInput) {

        for (int outterLoop = 0; outterLoop < dictionary.length ; outterLoop++){
            for (int innerLoop = 0 ; innerLoop < givenInput.length(); innerLoop++){
            //    if (dictionary[outterLoop].equals())
            }
        }
        return givenInput;
    }
}
